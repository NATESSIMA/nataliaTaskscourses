/*const URL  = 'https://github.com/fpaniaguaangular/fpaniaguaangular.github.io/blob/main/gamecovers/assets/gamecovers/gamecovers.json'
functionprocessData(data)


function doRequest(url){
    fetch(url)
    .then((response) => {
        
        if (!response.ok){
            throw new Error("network response wasnot ok");
        }
        else{
            return response. json();
        }})
    .then((data)=>{
            prodessData(data);//data en este caso es un array/*

            //data forEach(element)
        })
            .catch((error)=> console.error("fetch error:", error));
   
       
       const consolasVerticales =
       data.filter((consola)=> {consola. orientacion ==='portait'
return consola.orientacion==='portait';

 } )
const consolaNindendo = data.filter(consola =>{
    return consola. name.toUppercase(). includes("nindendo");
})
consolasNindendo.forEach(consola=>{
console.log(consola.name);
const consolas= data.map(consola =>
return sgl='insest into T_CONSOLAS set(name, value, orientation) values('${consola. name
return sgl;

})


function doRequest(url){}

doRequest (url) 
{
if(response.status !=200) { //200 es ok 
console.error(response.status)
throw new Error("Network response wasnot ok") ;


return response.json();

}}
*/





        